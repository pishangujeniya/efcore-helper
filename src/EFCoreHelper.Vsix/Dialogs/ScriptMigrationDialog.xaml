<platformUi:DialogWindow x:Class="EFCoreHelper.Vsix.Dialogs.ScriptMigrationDialog"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                          xmlns:shell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
                          xmlns:platformUi="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
                          Title="Generate SQL Script"
                          Width="560" Height="500"
                          WindowStartupLocation="CenterOwner"
                          ShowInTaskbar="False"
                          ResizeMode="NoResize"
                          Background="{DynamicResource {x:Static shell:VsBrushes.WindowKey}}"
                          Foreground="{DynamicResource {x:Static shell:VsBrushes.WindowTextKey}}">

  <platformUi:DialogWindow.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Resources/Styles.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </platformUi:DialogWindow.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Main Dialog Content -->
    <Grid Grid.Row="0" Margin="14,12,14,8">
      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <!-- Form Fields -->
      <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Style="{DynamicResource {x:Static shell:VsResourceKeys.ScrollViewerStyleKey}}">
        <StackPanel>
          <TextBlock Text="Target project (Migrations assembly):" Style="{StaticResource VsFieldLabelStyle}" />
          <ComboBox x:Name="CmbProject" Height="23" Style="{DynamicResource {x:Static shell:VsResourceKeys.ComboBoxStyleKey}}" DisplayMemberPath="Name" Margin="0,0,0,10" SelectionChanged="OnFieldChanged" />

          <TextBlock Text="Startup project (Executable / Web app):" Style="{StaticResource VsFieldLabelStyle}" />
          <ComboBox x:Name="CmbStartupProject" Height="23" Style="{DynamicResource {x:Static shell:VsResourceKeys.ComboBoxStyleKey}}" DisplayMemberPath="Name" Margin="0,0,0,10" SelectionChanged="OnFieldChanged" />

          <TextBlock Text="DbContext class:" Style="{StaticResource VsFieldLabelStyle}" />
          <ComboBox x:Name="CmbContext" Height="23" Style="{DynamicResource {x:Static shell:VsResourceKeys.ComboBoxStyleKey}}" ItemTemplate="{StaticResource DbContextItemTemplate}" Margin="0,0,0,10" SelectionChanged="OnFieldChanged" />

          <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="10" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
              <TextBlock Text="From migration (or 0):" Style="{StaticResource VsFieldLabelStyle}" />
              <ComboBox x:Name="CmbFromMigration" IsEditable="True" Height="23" Style="{DynamicResource {x:Static shell:VsResourceKeys.ComboBoxStyleKey}}" SelectionChanged="OnFieldChanged" TextBoxBase.TextChanged="OnFromTextChanged" />
            </StackPanel>
            <StackPanel Grid.Column="2">
              <TextBlock Text="To migration (empty = latest):" Style="{StaticResource VsFieldLabelStyle}" />
              <ComboBox x:Name="CmbToMigration" IsEditable="True" Height="23" Style="{DynamicResource {x:Static shell:VsResourceKeys.ComboBoxStyleKey}}" SelectionChanged="OnFieldChanged" TextBoxBase.TextChanged="OnToTextChanged" />
            </StackPanel>
          </Grid>

          <TextBlock Text="Output script file path (default: script.sql in startup project):" Style="{StaticResource VsFieldLabelStyle}" />
          <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="6" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtOutputPath" Grid.Column="0" Height="23" Style="{DynamicResource {x:Static shell:VsResourceKeys.TextBoxStyleKey}}" TextChanged="OnOutputTextChanged" />
            <Button Grid.Column="2" Content="Browse..." Height="23" Padding="8,2" Click="OnBrowseClicked" Style="{StaticResource SecondaryButtonStyle}" />
          </Grid>

          <StackPanel Orientation="Horizontal" Margin="0,2,0,6">
            <CheckBox x:Name="ChkIdempotent" Content="Idempotent script (--idempotent)" IsChecked="True" Style="{DynamicResource {x:Static shell:VsResourceKeys.CheckBoxStyleKey}}" Margin="0,0,16,0" Checked="OnFieldChanged" Unchecked="OnFieldChanged" />
            <CheckBox x:Name="ChkNoTransactions" Content="No transactions (--no-transactions)" Style="{DynamicResource {x:Static shell:VsResourceKeys.CheckBoxStyleKey}}" Checked="OnFieldChanged" Unchecked="OnFieldChanged" />
          </StackPanel>
        </StackPanel>
      </ScrollViewer>

      <!-- Command Line Preview -->
      <StackPanel Grid.Row="1" Margin="0,8,0,0">
        <TextBlock Text="Command preview:" Style="{StaticResource VsFieldLabelStyle}" />
        <TextBox x:Name="TxtCommandPreview" Style="{StaticResource CommandPreviewStyle}" Height="46" />
      </StackPanel>
    </Grid>

    <!-- Dialog Button Footer Bar -->
    <Border Grid.Row="1" Padding="14,10" Background="{DynamicResource {x:Static shell:VsBrushes.CommandBarMenuBackgroundGradientKey}}" BorderBrush="{DynamicResource {x:Static shell:VsBrushes.CommandBarBorderKey}}" BorderThickness="0,1,0,0">
      <Grid>
        <Button Content="Copy Command" HorizontalAlignment="Left" Click="OnCopyPreviewClicked" Style="{StaticResource SecondaryButtonStyle}" ToolTip="Copy CLI command to clipboard" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <Button x:Name="BtnGenerate" Content="Generate Script" IsDefault="True" Click="OnGenerateClicked" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0" Width="105" />
          <Button Content="Cancel" IsCancel="True" Click="OnCancelClicked" Style="{StaticResource SecondaryButtonStyle}" Width="75" />
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</platformUi:DialogWindow>
